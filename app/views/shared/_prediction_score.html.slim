td class=((defined?(tdclass) ? tdclass : "col-xs-1") + " " + (prediction.score==1? "success" : (prediction.score==2? "danger"  : "") )) 
  =prediction.match.knockout? ? prediction.winner_team.name : prediction.mainscores
  span  | 
  =prediction.reward
